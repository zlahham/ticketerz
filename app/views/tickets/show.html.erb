<div id="ticket">
  <header>

    <h1><%= link_to @project.name, project_path(@project)%></h1>
  </header>

  <header>
    <h2><%= @ticket.name %></h2>
  </header>

  <%= simple_format(@ticket.description) %>
</div>
